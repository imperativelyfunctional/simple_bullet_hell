import 'dart:async' as async;

import 'package:bullet_hell/Bullet1.dart';
import 'package:flame/components.dart';

extension BulletExtension on PositionComponent {
/*
  async.Timer shoot2({
    Vector2? scale,
  }) {
    return async.Timer.periodic(const Duration(milliseconds: 100), (timer) {
      var bullet = Bullet1()
        ..position = position
        ..scale = (scale == null) ? Vector2(1, 1) : scale;
      parent!.add(bullet);
    });
  }
*/
}
